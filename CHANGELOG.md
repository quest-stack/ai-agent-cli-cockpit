# 変更履歴

このファイルは利用者向けです。「何が変わって、自分に影響があるか」が分かることを
目的にしています。内部のリファクタリングなど、使う側に見えない変更は載せていません。

日付は JST です。

## 0.2.9 — 2026-09-25

- **Windowsの日本語入力を修正しました。** IME変換が入力欄から外れた場所に表示される問題と、変換確定後に文字が届かない場合がある問題を修正しました。実IMEでClaude・Codexの候補選択、連続入力、タブ復帰後の入力を確認しています。
- **0.2.8のコピー／貼り付け機能も含みます。** Ctrl+Cの中断／コピー切り替え、Codexのスクショ二重貼り付け修正、複数行貼り付けはそのまま使えます。

長時間利用や大量出力中など、すべてのIME・Windows環境での確認は継続します。

## 0.2.8 — 2026-09-24

- **英語版を別インストーラとして追加しました。** Windows ARM64／x64に対応し、画面・ヘルプ・ツアー・ダイアログ・通知を英語化しています。更新も同じ言語版を案内します。アプリ内の言語切り替えはありません。
- **最近使ったセッションも作業名を太字にしました。** フォルダー名は補足表示です。
- **Codexでスクショが二重に貼り付く経路を修正しました。** Ctrl+VをCLIへ送る前に止め、画像パスを1回だけ貼り付けます。
- **コマンド貼り付けに先行していた余分なCtrl+Vを除去しました。** クリップボード処理の失敗も通知します。テスターからの「コマンド貼り付け失敗」は操作詳細が未詳のため、同じ症状かは引き続き確認が必要です。
- **上部に「Ctrl+C: 中断／コピー」切り替えを追加しました。** コピー時は未選択でもCLIを中断せず、設定を保存します。
- **端末内のHTTP/HTTPSリンクを、確認後に既定ブラウザで開けるようにしました。** 開けなかった場合はエラーを表示します。

## 0.2.7 — 2026-09-21

### 使いやすくしたもの

- **起動中のセッションは、左一覧の×を押したときも終了前に確認します。** 入力待ち・待機中も対象です。キャンセルまたはEscで作業に戻れます。
- **案件・作業名を先に表示します。** 左一覧では案件名を太字、フォルダー名を補足にし、上部タブ・見出し・ペインも同じ順番に揃えました。
- **分割したペインを一時的に最大化できます。** ペイン右上のボタン、またはCtrl+Shift+Enterで切り替えます。戻すと元の配置・分割比率に復帰し、他のセッションも動き続けます。

### 調査中の問題

- Windowsの日本語入力で、変換表示が入力欄と無関係な左上に出て、確定文字が届かなくなる報告を調査中です。この版での修正には含まれていません。

## 0.2.6 — 2026-09-21

### 直したもの

- **Codex の処理中に文字を入力すると、画面がちらついたり、日本語の変換文字の
  表示位置が飛んだりする問題を修正しました。** 初回は問題がなくても、同じ
  セッションで2回目以降の会話を続けると再発するケースへの対策です。
  出力と入力位置を更新するタイミングを揃えました。

## 0.2.5 — 2026-09-19

### 直したもの

- **左の一覧からセッションを閉じると、中身のない「New Session」タブが
  残っていたのを直しました。** 閉じるたびに空のタブが積み上がり、タブ帯を
  埋めてしまう状態でした。中身が 1 つも無くなったタブは閉じるようにして
  います。

  **画面を分割して使っている場合、配置はそのままです。** 分割した片方を
  閉じても、もう片方が勝手に広がることはありません。空いたほうの枠には、
  そのまま次のセッションを入れられます。

- **タブが 5 枚ほどになると、選んだセッションのタブが画面の外に隠れて
  しまい、「✕」を押せなくなっていたのを直しました。** 左の一覧から選ぶと
  そのタブが選ばれた状態にはなるのに、タブ自体は右の見えない位置にある
  ため、閉じることも名前を確かめることもできませんでした。選んだタブが
  見える位置まで、タブ帯が自動で送られるようになります。

### 使いやすくしたもの

- 「✕」の押せる範囲を少し広げました。タブの幅は変えていません。
- **タブを中クリックしても閉じられる**ようにしました。「✕」を狙わなくて
  よいので、タブが多いときはこちらが早いです。
- 今開いているタブの文字を太くし、下線をタブの端まで引くようにしました。

## 0.2.4 — 2026-09-18

### 直したもの

- **「`Enter` で改行する」を有効にしていると、日本語の入力中に文字が打てなく
  なることがあったのを直しました。** 変換の途中で入力欄に何も出なくなり、
  半角/全角キーを押し直すと復旧する、という症状です。0.2.2 でこの設定を
  追加したときから入っていた不具合で、設定を有効にしている間だけ起きます。

  Windows の日本語入力は、変換中は `Enter` に限らず押されたキーをすべて
  「変換用のキー」として通知します。Cockpit 側でそれを `Enter` だと取り違え、
  文字キーまで横取りしていたのが原因でした。既定のまま（`Enter` で送信）
  お使いの場合は影響ありません。

## 0.2.3 — 2026-09-13

### 直したもの

- **Codex のペインで `Shift` + `Enter` を押しても改行できなかったのを直しました。**
  Claude Code と Codex では、改行として受け付ける制御シーケンスが違います。
  CLI ごとに送り分けるようにしました。設定で `Enter` と `Shift` + `Enter` を
  入れ替えている場合も同じように動きます。
- **Codex のペインだけ、入力していないときも画面が細かく明滅していたのを直しました。**
  Codex は待機中も入力欄に装飾のアニメーションを描き続けます。ペイン内では
  ちらつきとして見えるため、Codex を起動するときに装飾を止める指定を付けました。
  お使いの `~/.codex/config.toml` は変更しません。ターミナルから直接 `codex` を
  起動したときの見た目は変わりません。
- 日本語の変換中に、入力欄の高さが落ち着かないことがあったのを直しました。
- 文字の幅の測定が正しくなく、絵文字を含む行の幅がずれることがあったのを直しました。

## 0.2.2 — 2026-09-12

- 設定で `Enter` と `Shift` + `Enter` の役割を入れ替えられるようにしました。
  チャット欄の感覚に合わせたい場合に使えます。既定は変えていません
  （`Enter` が送信、`Shift` + `Enter` が改行）。
- 受付フォルダ（リモート起動）の場所を設定画面に表示するようにしました。

## 0.2.1 — 2026-09-06

- 古い WebGL の手動設定が残っていると表示が崩れることがあったため、
  自動で解除するようにしました。

## 0.2.0 — 2026-08-24

- 出るはずのない文字が CLI から届いたときに、診断ログへ記録するようにしました。
  文字化けの原因を追えるようにするためのものです。

## 0.1.9 — 2026-08-20

- 表示崩れの自動回避を入れました。画面が崩れても数分以内に戻ります。
  すぐ直したいときは `Ctrl` + `Shift` + `R` を押してください。
- 絵文字を含む行の文字がずれるのを直しました。

## それ以前

0.1.0 から 0.1.8 までは社内で使うための開発期間で、公開していません。
